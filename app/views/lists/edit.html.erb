<h1>編集画面</h1>

<%= form_with model:@list, url:list_update_path(@list.id), method: :patch, local:true do |f| %>
  
  <h4>タイトル</p>
  <%= f.text_field :title %>

  <P>本文</h4>
  <%=f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>